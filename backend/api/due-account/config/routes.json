{
  "routes": [
    {
      "method": "GET",
      "path": "/due-accounts",
      "handler": "due-account.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/due-accounts/count",
      "handler": "due-account.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/due-accounts/:id",
      "handler": "due-account.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/due-accounts",
      "handler": "due-account.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/due-accounts/:id",
      "handler": "due-account.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/due-accounts/:id",
      "handler": "due-account.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
